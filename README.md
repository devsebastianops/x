# x
This repository contains shared libraries used by rottweiler, watt-tf and other tools I develop in GoLang.

Currently the following packages are available:

- [logger](./logger) - Makes sure there is always a logger available and give the possibility of configuring `silent`, `verbose` and `format`.
- [parser](./parser) - Provides a parser interface with two different parsers currently available. 